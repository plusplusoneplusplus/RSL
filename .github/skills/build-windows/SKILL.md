---
name: build-windows
description: Build the native C++ RSL library on Windows.
---

# Build RSL on Windows

From the repository root, run:

```powershell
.\build.ps1
```

Report whether the build succeeded and include any error if it failed.
